######################################################################################
# (jdk installed env path to java and javac set)

# Build
./build

# Train (check usage)
./train [options] <path/to/dataset.csv>

# Predict
./predict

#####################################################################################
